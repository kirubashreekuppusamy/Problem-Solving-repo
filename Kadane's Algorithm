class Solution:
    def maxSubarraySum(self, arr):
        cur=arr[0]
        maxele=arr[0]
        for i in range(1,len(arr)):
            cur=max(arr[i],cur+arr[i])
            maxele=max(maxele,cur)
        return maxele
        # Code here