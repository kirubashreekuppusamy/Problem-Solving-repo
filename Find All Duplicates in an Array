class Solution:
    def findDuplicates(self, nums: List[int]) -> List[int]:
        numset=set()
        farr=[]
        for n in nums:
            if n not in numset:
                numset.add(n)
            else:
                farr.append(n)
        return farr
